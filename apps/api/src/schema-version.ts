// The newest migration this build of the API was written against. Updated by scripts/deploy/stamp-schema-version.mjs
// (run in the build) so a deployment cannot report "ready" against an older database schema.
export const EXPECTED_SCHEMA_VERSION = '20260925002000';
